Resources:
  UpdateClientQueuePolicy:
    Type: AWS::SQS::QueuePolicy
    Properties:
      PolicyDocument:
        Statement:
          - Effect: Allow
            Action:
              - SQS:*
            Resource: !GetAtt UpdateClientQueue.Arn
            Principal:
              AWS:
                - !Ref AWS::AccountId
          - Effect: Allow
            Principal:
              AWS:
                - '*'
            Action:
              - SQS:SendMessage
            Resource: !GetAtt UpdateClientQueue.Arn
            Condition:
              ArnLike:
                aws:SourceArn:
                  - !Ref AmountReachedTopic
      Queues:
        - !Ref UpdateClientQueue
